/**
 * React Scan Performance Monitoring Toggle (Native No-Op Stub)
 *
 * Native bundles (iOS/Android) resolve this file instead of `reactScan.web.ts`.
 * This prevents native runtime execution or bundling overhead from `react-scan`.
 */

export const REACT_SCAN_ENABLED = false;

export function initReactScan(): void {
  // No-op on native platforms
}

initReactScan();
